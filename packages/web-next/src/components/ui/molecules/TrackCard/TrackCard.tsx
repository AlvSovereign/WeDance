import { Release } from 'components/src/graphql/types'
import { FC } from 'react'

interface TrackCardProps {
  releases: Release
}

const TrackCard: FC<TrackCardProps> = ({ releases }) => {
  return <p>ierugfniuvejk</p>
}

export default TrackCard
